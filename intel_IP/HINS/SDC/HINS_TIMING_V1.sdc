# ----------------------------------------------------
# 檔案名稱: HINS_TIMING_V1.sdc
# 目的: 為 HINS_TOP_V1 專案設定 SDC 時序約束 (最終確定且最簡潔版)
# ----------------------------------------------------

# 1. 外部主時鐘約束 (CLOCK_100M)
# ----------------------------------------------------
set CLOCK_100M_PERIOD 10.000
create_clock -name {CLOCK_100M} -period $CLOCK_100M_PERIOD [get_ports {CLOCK_100M}]


# 2. 自動生成所有 PLL 時鐘約束
# ----------------------------------------------------
# TimeQuest 將自動掃描 PLL 並生成所有衍生時鐘，包括 c2 的 -45 度相位。
derive_pll_clocks


# 3. 處理 PLL 未鎖定狀態
# ----------------------------------------------------
# 這是為了防止在 PLL 尚未鎖定時，分析器使用這些不穩定的時鐘。
set_false_path -setup -hold -to [get_keepers {pll_inst|*|clk[*]}]